{
  "name": "tetris",
  "version": "1.0.0",
  "description": "Tetris Game in Snake Style",
  "scripts": {
    "build": "python3 build.py",
    "size": "echo 'Исходные:' && wc -c index.html tetris.css tetris.js && echo 'Бандл:' && wc -c index_bundle.html"
  },
  "devDependencies": {
    "uglify-js": "^3.19.3"
  }
}